# Production Smoke Test

- [ ] Health check: call `GET https://api.fintr4ck.click/api/v1/health` returns 200.
- [ ] Swagger "Try it out": open `https://api.fintr4ck.click/docs`, run a sample protected endpoint.
- [ ] Auth: register or login, receive token and can fetch `/auth/me`.
- [ ] Transactions: create, list, update, delete a transaction successfully.
- [ ] Session persistence: refresh UI after login and remain authenticated.
